package se.contribe.bookstore.api;

public interface BookStore {
    /**
     * Run the book store and evaluate the arguments
     * @param args command line arguments
     */
    void run(String[] args);
}
